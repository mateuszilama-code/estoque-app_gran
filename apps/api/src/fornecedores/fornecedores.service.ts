import { Injectable } from '@nestjs/common';

/**
 * Regras de negócio de Fornecedor (create com validação de CNPJ único,
 * findAll, findOne, update, remove) serão implementadas na Etapa 4.
 */
@Injectable()
export class FornecedoresService {}
