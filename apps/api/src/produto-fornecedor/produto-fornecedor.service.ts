import { Injectable } from '@nestjs/common';

/**
 * Regras de negócio da associação (associar validando duplicidade, desassociar,
 * listar fornecedores de um produto e produtos de um fornecedor) serão
 * implementadas na Etapa 6.
 */
@Injectable()
export class ProdutoFornecedorService {}
