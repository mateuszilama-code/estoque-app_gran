import { Injectable } from '@nestjs/common';

/**
 * Regras de negócio de Produto (create com validação de código de barras único,
 * findAll, findOne, update, remove) serão implementadas na Etapa 5.
 */
@Injectable()
export class ProdutosService {}
